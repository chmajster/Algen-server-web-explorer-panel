name: Temporary dynamic credentials form patch

on:
  push:
    branches:
      - feature/dynamic-credentials-form

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/dynamic-credentials-form
          fetch-depth: 0
      - name: Restore adjacent Hosts Manager components
        run: python .github/scripts/fix_dynamic_credentials_app.py
      - name: Commit repair
        run: |
          git add frontend/src/features/modules/hosts/HostsManagerApp.tsx
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git commit -m "fix: preserve Hosts Manager settings components"
          git push origin HEAD:feature/dynamic-credentials-form
