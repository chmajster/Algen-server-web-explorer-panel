name: Temporary credential accessibility repair

on:
  push:
    branches:
      - feature/dynamic-credentials-form

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/dynamic-credentials-form
          fetch-depth: 0
      - name: Add stable accessible names
        run: python .github/scripts/fix_credentials_aria.py
      - name: Commit accessibility fix
        run: |
          git add frontend/src/features/modules/hosts/HostsManagerApp.tsx
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git commit -m "fix: label dynamic credential fields explicitly"
          git push origin HEAD:feature/dynamic-credentials-form
