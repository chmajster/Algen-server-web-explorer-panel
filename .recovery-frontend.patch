diff --git a/frontend/src/app/App.tsx b/frontend/src/app/App.tsx
index a28498b..303c5b1 100644
--- a/frontend/src/app/App.tsx
+++ b/frontend/src/app/App.tsx
@@ -343,6 +343,11 @@ export function App({ reloadPage = reloadWindow }: { reloadPage?: () => void } =
       onRetry={() => {
         void api.runAutoUpdate(false).then((value) => { setDismissedFailureId(""); setUpdateProgress(value); }).catch(() => void refreshUpdateProgress());
       }}
+      onRecoveryStarted={(value) => {
+        setDismissedFailureId("");
+        setUpdateConnectionError(false);
+        setUpdateProgress(value);
+      }}
       onReturn={() => {
         setDismissedFailureId(updateProgress.id || "latest");
         window.history.replaceState({}, "", "/");
diff --git a/frontend/src/core/api/contracts.ts b/frontend/src/core/api/contracts.ts
index ea5c5e0..345df74 100644
--- a/frontend/src/core/api/contracts.ts
+++ b/frontend/src/core/api/contracts.ts
@@ -169,6 +169,7 @@ export type HostInfo = {
   gpus: string[];
   storage: UsageMetric & { path: string } | null;
 };
+export type UpdateVersion = { revision: string; name: string; kind: "tag" | "commit"; published_at: string | null };
 export type UpdateStatus = { branch: string; local: string; remote: string; installed_version?: string | null; available_version?: string | null; update_available: boolean; available?: boolean; error?: string; source?: string; source_url?: string; released_at?: number | null; checked_at?: number };
 export type UpdateBlocker = { id: string; type: string; status: "queued" | "running"; started_at: number | null; progress: number | null; description: string };
 export type UpdateStepStatus = "pending" | "running" | "success" | "failed" | "skipped";
diff --git a/frontend/src/features/settings/UpdateStatusPage.tsx b/frontend/src/features/settings/UpdateStatusPage.tsx
index e41b94d..b6592fc 100644
--- a/frontend/src/features/settings/UpdateStatusPage.tsx
+++ b/frontend/src/features/settings/UpdateStatusPage.tsx
@@ -18,10 +18,12 @@ import type {
   UpdateBlocker,
   UpdateCompletionNotice,
   UpdateProgress,
+  UpdateStart,
   UpdateStep,
 } from "../../api";
 import { request } from "../../core/api/transport";
 import type { Translate } from "../../app/types";
+import { UpdateVersionRecovery } from "./UpdateVersionRecovery";
 
 function timestamp(value: number | null | undefined) {
   return value ? new Date(value * 1000).toLocaleString() : "—";
@@ -112,6 +114,7 @@ export function UpdateStatusPage({
   canRetry = true,
   t,
   onRetry,
+  onRecoveryStarted,
   onReturn,
   onLogin,
 }: {
@@ -120,6 +123,7 @@ export function UpdateStatusPage({
   canRetry?: boolean;
   t: Translate;
   onRetry: () => void;
+  onRecoveryStarted?: (value: UpdateStart) => void;
   onReturn: () => void;
   onLogin: () => void;
 }) {
@@ -365,6 +369,16 @@ export function UpdateStatusPage({
           </section>
         </details>
 
+        {failed && canRetry && (
+          <UpdateVersionRecovery
+            key={value.id || "legacy-failure"}
+            failedUpdateId={value.id || null}
+            disconnected={connectionError}
+            t={t}
+            onStarted={onRecoveryStarted}
+          />
+        )}
+
         <footer className="update-status-footer">
           <p>
             <AlertTriangle />
diff --git a/frontend/src/features/settings/UpdateVersionRecovery.tsx b/frontend/src/features/settings/UpdateVersionRecovery.tsx
new file mode 100644
index 0000000..5346986
--- /dev/null
+++ b/frontend/src/features/settings/UpdateVersionRecovery.tsx
@@ -0,0 +1,102 @@
+import { useEffect, useRef, useState } from "react";
+import { HardDriveDownload, LoaderCircle } from "lucide-react";
+import type { Translate } from "../../app/types";
+import type { UpdateStart, UpdateVersion } from "../../core/api/contracts";
+import { updatesClient } from "../../modules/updates/api/client";
+
+export function UpdateVersionRecovery({ failedUpdateId, disconnected, t, onStarted }: {
+  failedUpdateId: string | null;
+  disconnected: boolean;
+  t: Translate;
+  onStarted?: (value: UpdateStart) => void;
+}) {
+  const [open, setOpen] = useState(false);
+  const [versions, setVersions] = useState<UpdateVersion[]>([]);
+  const [revision, setRevision] = useState("");
+  const [loading, setLoading] = useState(false);
+  const [submitting, setSubmitting] = useState(false);
+  const [error, setError] = useState("");
+  const [reload, setReload] = useState(0);
+  const inFlight = useRef(false);
+  const mounted = useRef(true);
+  const selectRef = useRef<HTMLSelectElement>(null);
+
+  useEffect(() => {
+    mounted.current = true;
+    return () => { mounted.current = false; };
+  }, []);
+
+  useEffect(() => {
+    if (!open) return;
+    let live = true;
+    setLoading(true);
+    setError("");
+    setRevision("");
+    setVersions([]);
+    void updatesClient.updateVersions().then((items) => {
+      if (live) setVersions(items);
+    }).catch((reason: unknown) => {
+      if (live) setError(reason instanceof Error ? reason.message : t("updateRecovery.loadError"));
+    }).finally(() => {
+      if (live) setLoading(false);
+    });
+    return () => { live = false; };
+  }, [open, reload, t]);
+
+  useEffect(() => {
+    if (open && !loading && versions.length > 0) selectRef.current?.focus();
+  }, [open, loading, versions]);
+
+  async function install() {
+    const selected = versions.find((item) => item.revision === revision);
+    if (!selected || loading || submitting || disconnected || inFlight.current) return;
+    const confirmation = t("updateRecovery.confirm").replace("{version}", `${selected.name} (${selected.revision.slice(0, 12)})`);
+    if (!window.confirm(confirmation)) return;
+    inFlight.current = true;
+    setSubmitting(true);
+    setError("");
+    try {
+      const result = await updatesClient.recoverUpdate(selected.revision, failedUpdateId);
+      if (!mounted.current) return;
+      onStarted?.(result);
+      if (!result.ok || result.state === "failed") {
+        setError(result.message || t("updateRecovery.installError"));
+      } else {
+        setOpen(false);
+      }
+    } catch (reason: unknown) {
+      if (mounted.current) setError(reason instanceof Error ? reason.message : t("updateRecovery.installError"));
+    } finally {
+      inFlight.current = false;
+      if (mounted.current) setSubmitting(false);
+    }
+  }
+
+  return <section className="update-version-recovery" aria-label={t("updateRecovery.title")}>
+    <button type="button" disabled={disconnected || submitting} aria-expanded={open} aria-controls="update-version-options" onClick={() => setOpen(!open)}>
+      <HardDriveDownload aria-hidden="true" />
+      {t("updateRecovery.title")}
+    </button>
+    {open && <div id="update-version-options" aria-busy={loading || submitting}>
+      <p>{t("updateRecovery.description")}</p>
+      <p>{t("updateRecovery.warning")}</p>
+      {error && <p role="alert">{error}</p>}
+      {loading && <p role="status"><LoaderCircle className="spin" aria-hidden="true" />{t("updateRecovery.loading")}</p>}
+      {!loading && versions.length === 0 && !error && <p role="status">{t("updateRecovery.empty")}</p>}
+      <label htmlFor="update-recovery-version">{t("updateRecovery.version")}</label>
+      <select id="update-recovery-version" ref={selectRef} value={revision} disabled={loading || submitting || disconnected || versions.length === 0} onChange={(event) => setRevision(event.target.value)}>
+        <option value="">{t("updateRecovery.choose")}</option>
+        {versions.map((item) => <option key={item.revision} value={item.revision}>
+          {t(`updateRecovery.kind.${item.kind}`)}: {item.name} — {item.revision.slice(0, 12)}{item.published_at ? ` (${item.published_at.slice(0, 10)})` : ""}
+        </option>)}
+      </select>
+      <div className="update-recovery-actions">
+        <button type="button" disabled={loading || submitting || disconnected} onClick={() => setReload((value) => value + 1)}>{t("updateRecovery.refresh")}</button>
+        <button className="button-primary" type="button" disabled={!revision || loading || submitting || disconnected} onClick={() => void install()}>
+          {submitting && <LoaderCircle className="spin" aria-hidden="true" />}
+          {t(submitting ? "updateRecovery.starting" : "updateRecovery.install")}
+        </button>
+      </div>
+    </div>}
+  </section>;
+}
diff --git a/frontend/src/locales/en-US.json b/frontend/src/locales/en-US.json
index 4c5108d..a373d20 100644
--- a/frontend/src/locales/en-US.json
+++ b/frontend/src/locales/en-US.json
@@ -3885,5 +3885,20 @@
   "hosts.credentials.field.gitUser": "Git username",
   "hosts.credentials.field.optional": "Optional field.",
   "hosts.credentials.wolNoSecret": "Wake-on-LAN does not require a secret.",
-  "hosts.credentials.wolNoSecretHint": "The MAC address and WOL parameters are configured in the host power profile."
+  "hosts.credentials.wolNoSecretHint": "The MAC address and WOL parameters are configured in the host power profile.",
+  "updateRecovery.title": "Install a selected repository version",
+  "updateRecovery.description": "Select a tag or one of the recent main branch revisions. Installation preserves the existing config.yaml and application data.",
+  "updateRecovery.warning": "An older version may be incompatible with the current configuration or database. Installation does not reverse data migrations. Back up your data before downgrading.",
+  "updateRecovery.confirm": "Install version {version}? Config.yaml will be preserved. The service may restart. An older version may be incompatible with your data; migrations will not be reversed.",
+  "updateRecovery.loading": "Loading repository versions…",
+  "updateRecovery.loadError": "Could not load repository versions.",
+  "updateRecovery.empty": "The repository returned no available versions.",
+  "updateRecovery.version": "Version to install",
+  "updateRecovery.choose": "Select a version…",
+  "updateRecovery.refresh": "Refresh version list",
+  "updateRecovery.install": "Install this version",
+  "updateRecovery.starting": "Starting installation…",
+  "updateRecovery.installError": "Could not start installation of the selected version.",
+  "updateRecovery.kind.tag": "Tag",
+  "updateRecovery.kind.commit": "Main revision"
 }
diff --git a/frontend/src/locales/pl-PL.json b/frontend/src/locales/pl-PL.json
index cbfd43b..f1a9697 100644
--- a/frontend/src/locales/pl-PL.json
+++ b/frontend/src/locales/pl-PL.json
@@ -3885,5 +3885,20 @@
   "hosts.credentials.field.gitUser": "Użytkownik Git",
   "hosts.credentials.field.optional": "Pole opcjonalne.",
   "hosts.credentials.wolNoSecret": "Wake-on-LAN nie wymaga sekretu.",
-  "hosts.credentials.wolNoSecretHint": "Adres MAC i parametry WOL są konfigurowane w profilu zasilania hosta."
+  "hosts.credentials.wolNoSecretHint": "Adres MAC i parametry WOL są konfigurowane w profilu zasilania hosta.",
+  "updateRecovery.title": "Zainstaluj wybraną wersję z repozytorium",
+  "updateRecovery.description": "Wybierz tag lub jedną z ostatnich rewizji gałęzi main. Instalacja zachowa istniejący plik config.yaml i dane aplikacji.",
+  "updateRecovery.warning": "Starsza wersja może być niezgodna z aktualną konfiguracją lub bazą danych. Instalacja nie cofa migracji danych. Przed obniżeniem wersji wykonaj kopię zapasową danych.",
+  "updateRecovery.confirm": "Zainstalować wersję {version}? Config.yaml zostanie zachowany. Usługa może zostać zrestartowana. Starsza wersja może być niezgodna z danymi; migracje nie zostaną cofnięte.",
+  "updateRecovery.loading": "Pobieranie wersji z repozytorium…",
+  "updateRecovery.loadError": "Nie można pobrać listy wersji.",
+  "updateRecovery.empty": "Repozytorium nie zwróciło dostępnych wersji.",
+  "updateRecovery.version": "Wersja do zainstalowania",
+  "updateRecovery.choose": "Wybierz wersję…",
+  "updateRecovery.refresh": "Odśwież listę wersji",
+  "updateRecovery.install": "Zainstaluj tę wersję",
+  "updateRecovery.starting": "Uruchamianie instalacji…",
+  "updateRecovery.installError": "Nie udało się uruchomić instalacji wybranej wersji.",
+  "updateRecovery.kind.tag": "Tag",
+  "updateRecovery.kind.commit": "Rewizja main"
 }
diff --git a/frontend/src/modules/updates/api/client.ts b/frontend/src/modules/updates/api/client.ts
index 96df397..6657a30 100644
--- a/frontend/src/modules/updates/api/client.ts
+++ b/frontend/src/modules/updates/api/client.ts
@@ -16,6 +16,7 @@ import type {
   UpdateStart,
   UpdateStatus,
   UpdateStep,
+  UpdateVersion,
 } from "../../../core/api/contracts";
 
 function nullableNumber(value: unknown): number | null {
@@ -185,6 +186,26 @@ function normalizeCompletion(value: unknown): UpdateCompletionNotice | null {
 }
 
 export const updatesClient = {
+  updateVersions: async (): Promise<UpdateVersion[]> => {
+    const value = asRecord(await request<unknown>("/api/admin/system/updates/versions"));
+    return asArray(value.versions).map((item): UpdateVersion => {
+      const source = asRecord(item);
+      return {
+        revision: asString(source.revision),
+        name: asString(source.name),
+        kind: source.kind === "tag" ? "tag" : "commit",
+        published_at: typeof source.published_at === "string" ? source.published_at : null,
+      };
+    }).filter((item) => /^[0-9a-f]{40}$/.test(item.revision));
+  },
+
+  recoverUpdate: async (revision: string, failedUpdateId: string | null) => normalizeStart(
+    await request<unknown>("/api/admin/system/updates/recover", {
+      method: "POST",
+      body: JSON.stringify({ revision, failed_update_id: failedUpdateId }),
+    }),
+  ),
+
   checkUpdates: async () => normalizeStatus(
     await request<unknown>("/api/admin/system/updates/check"),
   ),
diff --git a/frontend/src/styles/settings.css b/frontend/src/styles/settings.css
index f5f5ee6..a1a89a2 100644
--- a/frontend/src/styles/settings.css
+++ b/frontend/src/styles/settings.css
@@ -612,3 +612,11 @@
 @media (max-width: 520px) { .desktop .network-management-cards,.desktop .network-management-card > dl,.desktop .network-connectivity-form { grid-template-columns: 1fr; }.desktop .network-actionbar { align-items: stretch; flex-direction: column; }.desktop .network-transaction-banner button { flex: 1 1 100%; } }
 .desktop .settings-host-information { display: grid; gap: 0.75rem; margin-bottom: 0.875rem; }.desktop .settings-host-information > header { min-height: 3.875rem; display: flex; align-items: center; justify-content: space-between; gap: 1rem; padding: 0.8125rem 1rem; border: 1px solid var(--border-subtle); border-radius: 0.625rem; background: var(--surface-elevated); }.desktop .settings-host-information > header h3, .desktop .settings-host-information > header p { margin: 0; }.desktop .settings-host-information > header h3 { font-size: var(--font-size-md); }.desktop .settings-host-information > header p { margin-top: 0.1875rem; color: var(--text-muted); font-size: var(--font-size-xs); line-height: 1.4; }.desktop .settings-host-information > header button { flex: 0 0 auto; width: 2.25rem; height: 2.25rem; display: grid; place-items: center; border: 1px solid var(--border-subtle); border-radius: 0.4375rem; color: var(--text-secondary); background: var(--surface-secondary); }.desktop .settings-host-information > header button:hover:not(:disabled) { color: var(--text-primary); background: var(--surface-hover); }.desktop .settings-host-information > header button svg { width: 1rem; height: 1rem; }
 .desktop .settings-host-panels { display: grid; gap: 0.5rem; }.desktop .settings-host-panel { overflow: hidden; border: 1px solid var(--border-subtle); border-radius: 0.625rem; background: var(--surface-elevated); }.desktop .settings-host-panel > summary { min-height: 3.125rem; display: flex; align-items: center; gap: 0.625rem; padding: 0.5625rem 0.875rem; color: var(--text-primary); cursor: pointer; list-style: none; user-select: none; }.desktop .settings-host-panel > summary::-webkit-details-marker { display: none; }.desktop .settings-host-panel > summary:hover { background: var(--surface-hover); }.desktop .settings-host-panel > summary:focus-visible { outline: 2px solid var(--accent); outline-offset: -2px; }.desktop .settings-host-panel > summary > svg:first-child { width: 1.125rem; height: 1.125rem; color: var(--accent); }.desktop .settings-host-panel > summary strong { font-size: var(--font-size-sm); }.desktop .settings-host-chevron { width: 1rem; height: 1rem; margin-left: auto; color: var(--text-muted); transition: transform .14s ease; }.desktop .settings-host-panel[open] .settings-host-chevron { transform: rotate(180deg); }.desktop .settings-host-panel-content { border-top: 1px solid var(--border-subtle); }.desktop .settings-detail-pair { display: contents; }.desktop .settings-storage-meter { height: 0.4375rem; margin: 0.8125rem 1rem 1rem; overflow: hidden; border-radius: 0.3125rem; background: var(--surface-secondary); }.desktop .settings-storage-meter span { display: block; height: 100%; border-radius: inherit; background: var(--accent); }.desktop .settings-host-warning { margin: 0; color: var(--warning); font-size: var(--font-size-xs); }
+
+/* Recovery stays usable on the standalone failed-update screen, including mobile. */
+.update-version-recovery { display: grid; gap: 12px; margin-top: 18px; }
+.update-version-recovery > button { justify-self: start; max-width: 100%; white-space: normal; }
+.update-version-recovery > div { display: grid; gap: 10px; min-width: 0; }
+.update-version-recovery p { margin: 0; overflow-wrap: anywhere; }
+.update-version-recovery select { width: 100%; min-width: 0; max-width: 100%; }
+.update-recovery-actions { display: flex; flex-wrap: wrap; gap: 10px; }
