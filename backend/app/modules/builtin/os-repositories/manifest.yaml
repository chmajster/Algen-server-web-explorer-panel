id: os-repositories
name: OS Repositories
category: system
icon: package
permissions: [modules.view]
routers: [app.modules.os_repositories.router:router]
dependencies: [hosts-manager]
capabilities: [repositories, mirrors, synchronization, jobs]
actions: [sync, build, promote, rollback, cancel, retry]
jobs: [repository-sync, package-build]
system_capabilities: [apt, rpm]
