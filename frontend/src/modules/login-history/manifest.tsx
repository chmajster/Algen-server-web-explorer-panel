import { History } from "lucide-react";import type { FrontendModuleManifest } from "../../app/registry/moduleRegistry";import { LoginHistoryApp } from "./LoginHistoryApp";const manifest:FrontendModuleManifest={id:"login-history",labelKey:"Login History",icon:<History/>,category:"security",permission:"login_history.view",minWidth:960,minHeight:620,render:(c)=><LoginHistoryApp permissions={c.profile.permissions} language={c.profile.language} toast={c.toast}/>};export default manifest;
