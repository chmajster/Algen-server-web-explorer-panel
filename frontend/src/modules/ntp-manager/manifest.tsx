import { Clock } from "lucide-react";import type { FrontendModuleManifest } from "../../app/registry/moduleRegistry";import { NtpManagerApp } from "./NtpManagerApp";const manifest:FrontendModuleManifest={id:"ntp-manager",labelKey:"NTP Manager",icon:<Clock/>,category:"network",permission:"ntp.view",minWidth:900,minHeight:600,render:(c)=><NtpManagerApp permissions={c.profile.permissions} language={c.profile.language} toast={c.toast}/>};export default manifest;
