import { Route } from "lucide-react";import type { FrontendModuleManifest } from "../../app/registry/moduleRegistry";import { RoutingManagerApp } from "./RoutingManagerApp";const manifest:FrontendModuleManifest={id:"routing-manager",labelKey:"Routing Manager",icon:<Route/>,category:"network",permission:"routing.view",minWidth:1100,minHeight:680,render:(c)=><RoutingManagerApp permissions={c.profile.permissions} language={c.profile.language} toast={c.toast}/>};export default manifest;
